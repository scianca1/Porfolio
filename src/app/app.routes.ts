import { Routes } from '@angular/router';
import { HomeComponent } from './shared/templates/Pages/home/home.component';

export const routes: Routes = [
    {
        path:"",
        component: HomeComponent,
    }
];
